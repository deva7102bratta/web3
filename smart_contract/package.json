{
  "name": "smart_contract",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js"
  },
  "devDependencies": {
    "hardhat": "^3.0.6",
    "@nomicfoundation/hardhat-ethers": "^4.0.2"
  },
  "dependencies": {
    "ethers": "^6.15.0"
  }
}
